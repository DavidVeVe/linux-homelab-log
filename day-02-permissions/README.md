# Day 02 - Permissions

## Objective
Learn overall about permissions, types, restrictions, commands, security impact, structure.

## Why this matters (real-world)
1-3 bullets: where a mid/senior engineer uses this in production.

## Commands Used
```bash
git status
mkdir day-02-permissions
touch day-02-permissions/README.md day-02-permissions/commands.sh
wakeonlan MAC_Address
ping IP_Address
ssh servername
ls -l
echo $USER
echo "echo "test"" > config.sh
./config.sh
chmod +x config.sh
groups
sudo groupadd grouptest
sudo chgrp grouptest test.txt
chmod 770 test.txt
chown username test.txt